#include "game/GameManager.h"

int main(){
	RunGame();
	return 0;
}

/*
#ifdef AUDIO_ON
	InitAdioDevice();
endif

Usar namespaces.
Poner unicamente las funciones que se usen externamente en el header.
statics.
extern en vez de referencia.
*/